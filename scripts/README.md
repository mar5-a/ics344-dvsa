# Scripts

Put helper scripts here for:

- JWT decoding or token forging
- repeatable `curl` requests
- verification helpers
- log parsing or data cleanup

Keep secrets out of committed scripts. Load them from environment variables instead.
