# Evidence Layout

Each lesson folder is split into:

- `screenshots/` browser, terminal, CloudWatch, IAM, or AWS Console captures
- `notes/` command history, observations, and interpretation
- `code/` snippets, exported policies, diffs, or file references tied to that lesson

Store raw material here first, then choose the best pieces for the final report.
