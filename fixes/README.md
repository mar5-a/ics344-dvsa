# Fixes

Use this folder for consolidated fix artifacts that are easier to review across lessons, such as:

- copied Lambda snippets
- exported policy JSON
- before/after diffs
- sanitized config fragments

If a fix is specific to one lesson, also keep a copy under that lesson's `code/` folder.
